//Program for less than equal to 
#include <stdio.h>

int main() {
    int num1, num2;
    num1 = 20;
    num2 = 20;
    if (num1 <= num2)
    {
        printf("%d is less than %d\n", num1, num2);
    } else {
        printf("%d is equal than %d\n", num1, num2);
    }
    return 0;
}