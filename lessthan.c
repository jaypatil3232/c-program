//Program for greater than
#include <stdio.h>

int main() {
    int num1, num2;
    num1 = 10;
    num2 = 20;
    if (num1 < num2) {
        printf("%d is less than %d\n", num1, num2);
    } else {
        printf("%d is greater than %d\n", num1, num2);
    }
    return 0;
}